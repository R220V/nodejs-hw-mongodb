type: object
required:
  - email
  - password
properties:
  email:
    type: string
    description: User's email address
    format: email
    example: example@gmail.com

  password:
    type: string
    description: User's password
    format: password
    example: 123456
    minLength: 6